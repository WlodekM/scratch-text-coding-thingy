// a
gf {
    var a = 0
    // var vy = 0
    // control_forever() {
    //     vy = operator_subtract(vy, 1)
    //     if (operator_lt(motion_xposition(), operator_subtract(0,100))) {
    //         vy = 0
    //     }
    //     motion_changeyby(operator_subtract(0, vy))
    // }
    looks_say(a)
}