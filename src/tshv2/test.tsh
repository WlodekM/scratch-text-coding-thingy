#include <"blocks/js" "tw-blocks/blocks_vertical/control.js">

list test = { 1 2 }

gf {
    test::push("meow") // same as data_addtolist("meow", "test")
    looks_say(test::json) // same as skyhigh173JSON_json_vm_getlist("test")
    looks_say(test::at(2)) // 2
    looks_say(test::last::letter(2)) // e
}
